# Config files (secrets)
config.json
auth.json

# 本地运行时映射/备份（不入库）
config.json.bak
*.bak
service_ids.json

# IDE / tool settings
.claude/
.codex/
.workbuddy/
.pi-subagents/

# System files
.DS_Store

# 运行时数据与日志（程序生成，随源码重新生成）
cache_stats/
data/
logs/
proxy_*.log
vite-dev.*.log

# 绿色版可执行文件与打包产物（构建产物，随源码重新打包）
o2a-proxy.exe
o2a-desktop.exe
o2a-desktop-new.exe
o2a-desktop.exe.bak
mac/

# 前端构建输出
desktop/dist/

# pi subagent artifacts
.pi-subagents/

# pi 项目级 workflow 配置（本地保留，不入库）
.pi/
workflows/
WORKFLOWS.md

# Rust build artifacts
/target

# 引擎二进制（构建产物；桌面端 dev 模式从项目根解析）
/o2a-engine

# 实现文档（本地保留，不入库）
/docs
