{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["panel", "float", "float_*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
